function testfunction:
read A,B,C
%
  if nil then
        A := (hd C) ;
        B := (cons (hd A) B)
  else
    foreach A in B do
    	for (cons A B) do
			if (A and B and (A or B) ) then
				while A do
					nop
				od
			fi
		od
    od
  fi
%
write A,B,C,D

